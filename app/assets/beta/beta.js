(function() {
  const svCanvas = document.getElementById('saved-vector');
  const svContext = svCanvas.getContext('2d');
  const avCanvas = document.getElementById('active-vector');
  const avContext = avCanvas.getContext('2d');

  
})();
