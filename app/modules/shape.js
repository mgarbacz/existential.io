export class Shape {
  constructor(x, y, width, height, start, end, id) {
    this.x = x;
    this.y = y;
    this.width = width;
    this.height = height;
    this.start = start;
    this.end = end;
    this.id = id;
  }
}